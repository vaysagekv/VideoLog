Open the devcontainer to verify the environment builds and extensions load.
Build and run the Docker image to confirm the /health endpoint works.
Proceed with API endpoints and the video processing pipeline from the plan.